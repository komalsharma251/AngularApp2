import { DueDateLabelPipe } from './due-date-label-pipe';

describe('DueDateLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new DueDateLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
