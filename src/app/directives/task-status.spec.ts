import { TaskStatus } from './task-status';

describe('TaskStatus', () => {
  it('should create an instance', () => {
    const directive = new TaskStatus();
    expect(directive).toBeTruthy();
  });
});
